# Logical name -> physical layout. Reads use this in catalog.py.
bronze:
  orders:
    layer: bronze
    table: orders
    format: parquet

silver:
  orders:
    layer: silver
    table: orders
    format: parquet

gold:
  orders_daily:
    layer: gold
    table: orders_daily
    format: parquet
